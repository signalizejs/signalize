version: '3'
services:
    server:
        container_name: inslands
        build: .
        working_dir: /var/www
        volumes:
            - ./.:/var/www:delegated
        ports:
            # Vite Dev Server
            - 5173:5173
            # Vite Preview
            - 4173:4173
            # Playwright Report
            - 9323:9323
        tty: true
